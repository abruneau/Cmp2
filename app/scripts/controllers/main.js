'use strict';

/**
 * @memberof cmp2App
 * @ngdoc controller
 * @name MainCtrl
 * @description
 * # MainCtrl
 * Controller of the cmp2App
 */
angular.module('cmp2App').controller('MainCtrl', function() {
  // if (!salesForce.sfSet()) {
  // 	$window.location.href = '#/profile';
  // }
});
