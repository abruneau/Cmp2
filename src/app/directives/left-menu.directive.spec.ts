import { LeftMenuDirective } from './left-menu.directive';

describe('LeftMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftMenuDirective();
    expect(directive).toBeTruthy();
  });
});
