import { OpenExternalDirective } from './open-external.directive';

describe('OpenExternalDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenExternalDirective();
    expect(directive).toBeTruthy();
  });
});
