import { SubTreeToggleDirective } from './sub-tree-toggle.directive';

describe('SubTreeToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new SubTreeToggleDirective();
    expect(directive).toBeTruthy();
  });
});
